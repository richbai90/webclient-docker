<?php

	//--
	//-- return callrefs + status associated to issue record from system db
	$sqlDatabase = "sw_systemdb";
	$sqlCommand  = "SELECT callref, status FROM opencall WHERE issueref = '" . prepareForSql($POST['ir']) ."'";

?>