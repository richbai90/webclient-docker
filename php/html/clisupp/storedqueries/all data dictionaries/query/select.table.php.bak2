<?php

	//-- select records for a given table
	//-- this relies on analysts db right permissions on the table i.e must have select right

	//--
	$sqlDatabase = "swdata";
	if($_POST['cdb']=="1")	$sqlDatabase = "sw_systemdb";
	$sqlCommand =  "SELECT * FROM " . UC(swfc_tablename());

?>