<?php
	//--
	//-- note {} tags sql will only be included if :[wf] param has a value - this way can  choose when to include optional sql statements
	$sqlDatabase = "swdata";
	$sqlCommand = "Select count(*) from BPM_WORKFLOW where FLG_ACTIVE=1 and CALLCLASS=':[cls]' {and pk_workflow_id=':[wf]'}";
?>